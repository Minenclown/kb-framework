# KB Framework Konfiguration
# Kopieren zu config.py und anpassen

# Datenbank
DB_PATH = "knowledge.db"  # SQLite DB
CHROMA_PATH = ".knowledge/chroma_db/"  # ChromaDB Vektoren

# Bibliothek
LIBRARY_PATH = "library/"  # Wo die Dokumente liegen

# Such-Parameter
DEFAULT_LIMIT = 20
SEMANTIC_WEIGHT = 0.6
KEYWORD_WEIGHT = 0.4

# OCR (für bildbasierte PDFs)
OCR_LANGUAGES = ["de", "en"]
OCR_GPU = False  # True wenn GPU verfügbar
