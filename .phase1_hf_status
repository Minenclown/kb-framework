Phase 1: HuggingFace-Integration Grundstruktur - COMPLETE

✅ 1. LLMProvider erweitert
   - kb/llm/engine/base.py: LLMProvider.HUGGINGFACE = "huggingface" hinzugefügt

✅ 2. LLMConfig erweitert
   - kb/llm/config.py: 9 neue Config-Felder:
     - model_source (default: "ollama")
     - hf_model_name (default: "google/gemma-2-2b-it")
     - hf_quantization (optional: "4bit", "8bit", None)
     - hf_device (default: "auto")
     - hf_cache_dir (optional)
     - hf_token (optional, masked in to_dict)
     - hf_revision (default: "main")
     - hf_trust_remote_code (default: False)
     - hf_torch_dtype (default: "auto")
     - hf_offload_folder (optional)
   - Helper-Methoden: _resolve_optional(), _resolve_bool()
   - Validierung: _validate_hf_config()
   - to_dict(), __repr__(), __str__() aktualisiert

✅ 3. TransformersEngine Skeleton
   - kb/llm/engine/transformers_engine.py erstellt
   - Klasse TransformersEngine(BaseLLMEngine)
   - Exceptions: TransformersEngineError, TransformersModelLoadError, TransformersGenerationError
   - Methoden: __init__, is_available, get_model_name, get_provider, generate, generate_async
   - generate/generate_async raise TransformersGenerationError (Phase 3 stub)
   - supports_streaming=True, supports_async=True

✅ 4. Engine Factory
   - kb/llm/engine/factory.py erstellt
   - create_engine(config) → OllamaEngine oder TransformersEngine basierend auf config.model_source
   - Dependency-Check für torch/transformers

✅ 5. Package Exports
   - kb/llm/engine/__init__.py aktualisiert
   - Exportiert: TransformersEngine, TransformersEngineError, TransformersModelLoadError, TransformersGenerationError, create_engine

✅ 6. Requirements
   - requirements-transformers.txt erstellt (torch, transformers, accelerate, bitsandbytes)

✅ 7. Kompilierung
   - Alle .py Dateien kompilieren ohne Fehler (python3 -m py_compile)

✅ 8. Import-Test
   - from kb.llm.engine import TransformersEngine, create_engine → OK
   - LLMConfig(model_source="huggingface", hf_model_name="...", ...) → OK
   - TransformersEngine(config) → OK
   - engine.get_provider() == LLMProvider.HUGGINGFACE → OK
   - engine.is_available() → OK (torch + transformers installiert)
   - engine.generate() raises TransformersGenerationError → OK
   - Config-Validierung für hf_ Felder → OK
